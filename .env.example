# Shirushi Configuration

# OpenAI API Key (required for real AI, leave empty for mock mode)
OPENAI_API_KEY=

# Nostr Relays (comma-separated)
NOSTR_RELAYS=wss://relay.damus.io,wss://nos.lol

# Web Dashboard
WEB_ADDR=:8080

# Agent Configuration File
AGENTS_CONFIG=agents.yaml

# Filter Mode: "trusted" only processes jobs from your own agents, "public" processes all
FILTER_MODE=trusted
